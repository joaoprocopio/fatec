const sum = (a: number, b: number): number => {
  return a + b
}

const sub = (a: number, b: number): number => {
  return a - b
}

let result = sum(1, 2)
console.log(result)

result = sub(5, 4)
console.log(result)
