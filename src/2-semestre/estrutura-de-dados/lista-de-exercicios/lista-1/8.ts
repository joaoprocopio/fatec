// Escreva um programa, utilizando o bloco for, que imprima todos os múltiplos de 3 contidos entre 0 e 100, inclusive. Os valores devem ser impressos em ordem crescente.

for (let iter = 0; iter < 100; iter += 3) {
  console.log(iter)
}
